# Infrastructure / deployment configuration
# Copy to .env and customize for your environment

# Base directory for data/, models/ (default: /)
GREETING_BASE_DIR=/

# Base directory for config.yaml (default: /config/)
GREETING_CONFIG_DIR=/config/
